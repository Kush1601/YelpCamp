# PostgreSQL (PostGIS) — docker compose: npm run db:migrate && npm run db:seed
DATABASE_URL=postgres://yelpcamp:yelpcamp@localhost:5432/yelpcamp

# Clerk — https://dashboard.clerk.com
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in

# Anthropic — Claude Haiku powers semantic query expansion for similar search (optional)
ANTHROPIC_API_KEY=

